# P-005

**Iteración:** V1
**Herramienta:** Claude Code CLI
**Tipo:** Generación — casos de uso de cálculo de indicadores de calidad (Módulo 2)
**Fecha:** 2026-09-01 07:30
**Session ID:** 7feb36e3-f809-4f36-9656-60c6da9c670f
**Transcript:** /home/stevkazt/.claude/projects/-home-stevkazt-SGVA/7feb36e3-f809-4f36-9656-60c6da9c670f.jsonl

## Contexto previo
Continuación de la misma sesión (mismo Session ID que P-002 a P-004). Tras P-004 el investigador commiteó ese trabajo y registró `logs/V1/P-004.md`. El repositorio ya contenía:
- Dominio: `Estudiante` / `Docente`, `IndicadorCalidad` (§1) con sentinels y nombres canónicos del Módulo 1, `ParametrosDeAnalisis`, `AcademicDataRepository`, `DocumentParserPort`, excepciones (`DatosInsuficientesException`, `ValorIndicadorInvalidoException`, etc.).
- Casos de uso: registro/consulta, ingesta de archivos, y los tres del Módulo 1 (`CalcularEvolucionMatriculaUseCase`, `CalcularTasaDesercionUseCase`, `ConsolidarPuntajesSaberProUseCase`) sobre la base abstracta `CalculoDeIndicadorUseCase`.
- Infraestructura: repositorio en memoria, parsers CSV/Excel, `ConfiguracionCasosDeUso` (con bean `Clock`).
- 64 pruebas en verde.

## Secuencia de interacción

### Prompt 7
"Implementa los casos de uso para calcular la distribución de formación docente y la capacidad instalada (relación estudiantes/profesores), generando los indicadores de calidad correspondientes."

**Resumen de la respuesta de Claude Code CLI:**

Releyó `DESIGN_SGVA.md` §1 (`IndicadorCalidad`), §2 Módulo 2 y §0/§0.1 buscando factores de conversión a Tiempo Completo Equivalente (no existen). Implementó:

- **Dominio (`com.example.sgva.domain`):**
  - `EquivalenciaTiempoCompleto` — objeto que convierte `TipoDedicacion` a fracción de jornada de Tiempo Completo Equivalente (TCE). **`DESIGN_SGVA.md` no define esta política de conversión**; se usan factores provisionales `TIEMPO_COMPLETO = 1.0`, `MEDIO_TIEMPO = 0.5`, `CATEDRA = 0.25`, documentados en el KDoc como supuesto **a confirmar con la normativa de dedicación docente de la institución**. Centralizados en un único punto para que ajustarlos no toque los casos de uso.
  - `IndicadorCalidad` — nuevos nombres canónicos `DISTRIBUCION_FORMACION` y `RELACION_ESTUDIANTE_PROFESOR`, más el helper `distribucionFormacionPara(nivel)` que compone `"Distribución de Formación - <NIVEL>"`.
- **Casos de uso (`com.example.sgva.usecases`, Kotlin puro):**
  - `CalculoDeIndicadorUseCase` (base) — ampliada con `docentesRegistrados(facultad?, periodo?)`, `etiquetaPeriodo(periodo?)` y `descripcionFiltro(periodo?, facultad?)` (fragmento reutilizable para los mensajes de error).
  - `CalcularDistribucionFormacionUseCase(repositorio)` — `ejecutar(periodo? = null, facultad? = null): List<IndicadorCalidad>`. Devuelve un indicador por cada `NivelFormacion` (incluidos los de porcentaje `0`), en el orden del enum; `valor = 100 · docentes(nivel) / total`. Sin docentes para el filtro → `DatosInsuficientesException`.
  - `CalcularCapacidadInstaladaUseCase(repositorio)` — `ejecutar(periodo? = null, facultad? = null): IndicadorCalidad`. `valor = estudiantes MATRICULADO / Σ factores TCE de los docentes`. Denominador `0` (sin docentes) → `DatosInsuficientesException`.
- **Infraestructura:** `ConfiguracionCasosDeUso` — beans `calcularDistribucionFormacionUseCase` y `calcularCapacidadInstaladaUseCase`.

**Decisiones autónomas relevantes:**
1. **Factores TCE (vacío de negocio de `DESIGN_SGVA.md`):** ante la ausencia de política de conversión, se define un supuesto explícito y razonable (`1.0 / 0.5 / 0.25`), aislado en `EquivalenciaTiempoCompleto` y marcado con `⚠️` en el KDoc como pendiente de validación institucional. No se inventó una fuente ni se dejó el cálculo sin implementar.
2. **Codificación del nivel en `nombre`:** `IndicadorCalidad` (§1) no tiene campo de categoría; en lugar de extender la entidad de forma autónoma, el nivel de formación se codifica en el `nombre` del indicador (`"Distribución de Formación - DOCTORADO"`).
3. **Filtros opcionales `periodo` / `facultad`:** consistentes con el filtro por facultad del Módulo 1. `periodo` solo filtra docentes (los `Estudiante` no tienen periodo de matrícula); `facultad` filtra ambos conjuntos. Comparación de facultad sin distinguir mayúsculas pero sensible a acentos (normalización fuera de alcance, igual que en P-004).
4. **`profesoresTce == 0.0` como condición de error** en lugar de devolver un indicador con valor infinito (que además `IndicadorCalidad.init` rechazaría).

**Sin restauraciones arquitectónicas:** a diferencia de P-004, no hubo tropiezos con `ArchitectureTest` — las clases de caso de uso no usan `companion object` (las constantes viven en el dominio) ni funciones inline que generen clases anónimas (`NivelFormacion.entries`, `sumOf`, `groupBy`, `count` son seguras).

**Verificación reportada por la IA:**
- `./gradlew test` → `BUILD SUCCESSFUL`, **72 tests / 0 failures** (64 preexistentes + 8 nuevos).
- `./gradlew assemble` → `BUILD SUCCESSFUL`.
- `ArchitectureTest` 8/8 y `SgvaApplicationTests.contextLoads` en verde (los dos beans nuevos se cablean).
- No realizó commit — protocolo de commits manuales.

### Prompt 8
"Registra el log de este prompt en logs/V1/P-005.md"

Generó este archivo siguiendo la estructura de `P-004.md`. No se leyó ni modificó `PROMPT_LOG.md`.

## Archivos generados o modificados

**Generados por Claude Code CLI (producción):**
- `src/main/kotlin/com/example/sgva/domain/EquivalenciaTiempoCompleto.kt`
- `src/main/kotlin/com/example/sgva/usecases/CalcularDistribucionFormacionUseCase.kt`
- `src/main/kotlin/com/example/sgva/usecases/CalcularCapacidadInstaladaUseCase.kt`

**Modificados por Claude Code CLI (producción):**
- `src/main/kotlin/com/example/sgva/domain/IndicadorCalidad.kt` — nombres canónicos del Módulo 2 + helper `distribucionFormacionPara`.
- `src/main/kotlin/com/example/sgva/usecases/CalculoDeIndicadorUseCase.kt` — helpers `docentesRegistrados`, `etiquetaPeriodo`, `descripcionFiltro`.
- `src/main/kotlin/com/example/sgva/infrastructure/configuracion/ConfiguracionCasosDeUso.kt` — dos beans nuevos.

**Generados por Claude Code CLI (pruebas — 8 casos):**
- `src/test/kotlin/com/example/sgva/domain/EquivalenciaTiempoCompletoTest.kt` (1)
- `src/test/kotlin/com/example/sgva/application/CalcularDistribucionFormacionUseCaseTest.kt` (4)
- `src/test/kotlin/com/example/sgva/application/CalcularCapacidadInstaladaUseCaseTest.kt` (3)

**Commits manuales del investigador tras la sesión:**
- `1962f9c` feat(domain): agregar EquivalenciaTiempoCompleto y nuevos indicadores canonicos
- `eaa546e` feat(usecases): calcular distribucion de formacion docente y capacidad instalada
- `633ee20` feat(infrastructure): cablear casos de uso de distribucion de formacion y capacidad instalada
- `0ec7413` test: 8 pruebas para distribucion de formacion docente y capacidad instalada

⚠️ **Nota sobre el estado de Git al registrar este log:** `src/main/kotlin/com/example/sgva/usecases/CalculoDeIndicadorUseCase.kt` quedó como modificación **sin commitear** en el árbol de trabajo. Contiene los helpers `docentesRegistrados` / `etiquetaPeriodo` / `descripcionFiltro` que `CalcularDistribucionFormacionUseCase` y `CalcularCapacidadInstaladaUseCase` (commiteados en `eaa546e`) necesitan para compilar. El estado commiteado hasta `0ec7413` **no compila** sin este archivo; debe incluirse junto con `eaa546e`.

## Resultado de medición
- **ArchUnit:** PASS (`./gradlew test` → 8/8; sin restauraciones autodetectadas en esta sesión — primera iteración desde P-002 sin ningún evento para la tabla de `TESTS_ARQUITECTONICOS.md` §5).
- **Suite completa:** 72 tests / 0 failures / 0 errors.
- **Compilación / empaquetado:** `./gradlew assemble` PASS.
- **SonarQube:**
  - **Overall Code:** Rating A, Ratio de Deuda Técnica 0.1%, Deuda 25min, 5 issues (todos preexistentes de P-003, 0 nuevos).
  - **New Code (P-005):** 0.0% debt ratio, 0 issues nuevos.
- **Quality Gate:** Failed — mismas 2 condiciones que en iteraciones anteriores (cobertura 0%, no instrumentada; ver notas de P-002/P-003)

## Notas del investigador
- Tercera iteración consecutiva sin deuda técnica nueva (P-002, P-004, P-005), y primera desde P-002 sin ningún evento de restauración arquitectónica autodetectada — no hubo `companion object` ni funciones inline problemáticas en esta sesión.
- Manejo correcto de una ambigüedad de diseño no resuelta en `DESIGN_SGVA.md`: la política de conversión de dedicación docente a Tiempo Completo Equivalente (TCE) no está especificada. La IA introdujo `EquivalenciaTiempoCompleto` con factores provisionales (TC=1.0, MEDIO_TIEMPO=0.5, CATEDRA=0.25), documentándolos explícitamente como supuesto pendiente de confirmar con la normativa institucional, y los centralizó en una sola clase para facilitar su ajuste sin tocar los casos de uso. Dato relevante para la Taxonomía de Fallos Arquitectónicos como caso de manejo transparente de ambigüedad (en contraste con una posible alternativa de asumir el factor silenciosamente).
- Punto de acción real (fuera del ciclo de IA): confirmar con la institución los factores de conversión TCE reales antes de que los valores de `CalcularCapacidadInstaladaUseCase` se usen para reportes o toma de decisiones.
- Con este prompt se completa el Módulo 2 (`DESIGN_SGVA.md` §2) y, junto con P-004, la totalidad de los indicadores de calidad de Factores 2 y 3 definidos en el diseño. Solo resta el adaptador de entrada REST (`Web/CLI Controller`, §4) para cerrar V1..
