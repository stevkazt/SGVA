# P-002

**Iteración:** V1
**Herramienta:** Claude Code CLI
**Tipo:** Generación — capa de casos de uso, puerto de persistencia y adaptador en memoria
**Fecha:** 2026-09-01 05:20
**Session ID:** 7feb36e3-f809-4f36-9656-60c6da9c670f
**Transcript:** /home/stevkazt/.claude/projects/-home-stevkazt-SGVA/7feb36e3-f809-4f36-9656-60c6da9c670f.jsonl

## Contexto previo
Repositorio tras P-001: la capa de dominio contiene las entidades `Estudiante` y `Docente` con validación de formato/rango en el bloque `init`, sus enums, `ReglasDeFormato` y `ExcepcionesDominio`. El investigador ya había commiteado P-001, corregido el fixture `datos_estudiantes_test.csv` al formato `AAAAS` (commit `2c3a212`) y actualizado el índice de `PROMPT_LOG.md` (commit `4e32d17`). Las capas `usecases` e `infrastructure` estaban vacías (solo existían los paquetes conceptualmente).

## Secuencia de interacción

### Prompt 1
"Implementa los casos de uso para registrar y consultar estudiantes y docentes. Define los puertos de persistencia necesarios en la capa de dominio y una implementación en memoria en la capa de infraestructura para esta versión."

**Resumen de la respuesta de Claude Code CLI:**

Exploró el estado del repositorio (entidades, `ArchitectureTest.kt`, `DESIGN_SGVA.md`, log de P-001) antes de generar código y respetó el aislamiento de capas y las convenciones de `CLAUDE.md`.

- **Dominio (`com.example.sgva.domain`):**
  - `AcademicDataRepository.kt` — puerto de salida (Outbound Port) de persistencia con operaciones `guardar` / `buscarPorId` / `listar` para `Estudiante` y `Docente`. **Decisión de diseño explícita:** se usó el nombre y el alcance único que `DESIGN_SGVA.md` §3 asigna a este puerto, en lugar de segregar en `EstudianteRepository` / `DocenteRepository`, para no alterar de forma autónoma un límite arquitectónico fijado por el diseño. El puerto solo almacena/consulta; no aplica reglas de negocio.
  - `ExcepcionesDominio.kt` — se añadió `EntidadDuplicadaException` (extiende `DominioException`). El registro es una operación de alta; modificar un registro existente se deja fuera del alcance de esta versión.
- **Casos de uso (`com.example.sgva.usecases`, Kotlin puro, sin Spring):**
  - `RegistrarEstudianteUseCase`, `RegistrarDocenteUseCase` — método `ejecutar(entidad)`; aplican la única regla de aplicación pendiente (unicidad del `id`), delegando la validación de formato/rango al bloque `init` de la entidad.
  - `ConsultarEstudiantesUseCase`, `ConsultarDocentesUseCase` — `listarTodos()` y `buscarPorId(id): T?`, sin efectos sobre el estado.
- **Infraestructura (`com.example.sgva.infrastructure`):**
  - `persistence/AcademicDataRepositoryEnMemoria` — adaptador anotado con `@Repository`; `LinkedHashMap` sincronizado para preservar el orden de inserción de forma determinista (pruebas y Dashboard). Sin durabilidad entre ejecuciones.
  - `configuracion/ConfiguracionCasosDeUso` — clase `@Configuration` con métodos `@Bean` que ensamblan los cuatro casos de uso, inyectándoles el puerto. Los casos de uso no se anotan con `@Component`/`@Service` (lo prohíbe `CLAUDE.md` y lo verifica `ArchitectureTest`); es la infraestructura la que los compone.
- **Pruebas — 12 casos nuevos:**
  - `com.example.sgva.application.GestionEstudiantesUseCaseTest` (5) y `GestionDocentesUseCaseTest` (4), usando un doble de prueba `RepositorioAcademicoFalso` (implementación en memoria del puerto) para no acoplar las pruebas de casos de uso al adaptador de infraestructura.
  - `com.example.sgva.infrastructure.persistence.AcademicDataRepositoryEnMemoriaTest` (3) — verifica persistencia, orden de inserción y reemplazo por `id`.
- **Decisión autónoma relevante:** al ejecutar la suite, Claude Code CLI detectó que `ArchitectureTest.kt` importa las clases de test junto con las de producción (`ClassFileImporter().importPackages("com.example.sgva")` sin `ImportOption.DO_NOT_INCLUDE_TESTS`). Por ello, colocar las pruebas de casos de uso en el paquete `com.example.sgva.usecases` rompía la regla *"las clases de casos de uso deben finalizar en UseCase"* (aplica a toda clase no-interface del paquete). En lugar de modificar `ArchitectureTest.kt` —archivo que el investigador gestiona manualmente—, reubicó las pruebas en un paquete de test nuevo `com.example.sgva.application`, que no coincide con ninguna regla de capa. Una primera ejecución falló con esa violación y la corrección se aplicó en la misma sesión.
- **Verificación reportada por la IA:** `./gradlew test --offline` → `BUILD SUCCESSFUL`, 33 tests / 0 failures (incluye `ArchitectureTest` 8/8 y `SgvaApplicationTests.contextLoads`, que confirma que el contexto de Spring cablea los beans correctamente).
- No realizó commit — consistente con el protocolo de commits manuales del proyecto.

### Prompt 2
"Registra el log de este prompt en logs/V1"

Generó este archivo (`logs/V1/P-002.md`) siguiendo la estructura de `P-001.md`. No se leyó ni modificó `PROMPT_LOG.md` (prohibido por `CLAUDE.md`; su fila índice la inserta el hook `SessionEnd`).

## Archivos generados o modificados

**Generados por Claude Code CLI (código de producción):**
- `src/main/kotlin/com/example/sgva/domain/AcademicDataRepository.kt`
- `src/main/kotlin/com/example/sgva/usecases/RegistrarEstudianteUseCase.kt`
- `src/main/kotlin/com/example/sgva/usecases/ConsultarEstudiantesUseCase.kt`
- `src/main/kotlin/com/example/sgva/usecases/RegistrarDocenteUseCase.kt`
- `src/main/kotlin/com/example/sgva/usecases/ConsultarDocentesUseCase.kt`
- `src/main/kotlin/com/example/sgva/infrastructure/persistence/AcademicDataRepositoryEnMemoria.kt`
- `src/main/kotlin/com/example/sgva/infrastructure/configuracion/ConfiguracionCasosDeUso.kt`

**Modificado por Claude Code CLI:**
- `src/main/kotlin/com/example/sgva/domain/ExcepcionesDominio.kt` — se añadió `EntidadDuplicadaException`.

**Generados por Claude Code CLI (pruebas):**
- `src/test/kotlin/com/example/sgva/application/GestionEstudiantesUseCaseTest.kt`
- `src/test/kotlin/com/example/sgva/application/GestionDocentesUseCaseTest.kt`
- `src/test/kotlin/com/example/sgva/application/RepositorioAcademicoFalso.kt`
- `src/test/kotlin/com/example/sgva/infrastructure/persistence/AcademicDataRepositoryEnMemoriaTest.kt`

**Nota:** durante la sesión se creó y luego eliminó una primera versión de las pruebas de casos de uso bajo `src/test/kotlin/com/example/sgva/usecases/` (ver decisión autónoma); no quedó rastro en el árbol de trabajo final.

**Documentación (este prompt):**
- `logs/V1/P-002.md` — nuevo.

**Commits manuales del investigador tras la sesión:**
- `3953a3c` feat(domain): agregar puerto AcademicDataRepository y EntidadDuplicadaException
- `98c29f1` feat(usecases): registrar y consultar estudiantes y docentes
- `8f6315b` feat(infrastructure): adaptador en memoria y configuracion de casos de uso
- `31c205d` test: 12 pruebas para casos de uso y repositorio en memoria

## Resultado de medición
- **ArchUnit:** PASS (`./gradlew test --offline` → `BUILD SUCCESSFUL`; 8/8 pruebas de `ArchitectureTest.kt` sin fallos, tras corregir la ubicación de las pruebas de casos de uso).
- **Suite completa:** 33 tests / 0 failures / 0 errors (12 de P-001 en dominio + 8 ArchUnit + 1 contexto Spring + 12 nuevos de esta sesión).
- **SonarQube:** Rating **A**, Ratio de Deuda Técnica **0.0%** (New Code, commits `3953a3c`..`31c205d`). 2 issues abiertos a nivel de proyecto completo (Overall Code), ambos preexistentes en `build.gradle.kts` ("Group dependencies by their destination", "Do not hardcode version numbers", severidad Medium, timestamp de hace 1 mes) — **0 issues nuevos** introducidos por esta iteración.
- **Quality Gate:** Failed — únicamente por la condición de cobertura (0.0% < 80.0% requerido). La cobertura de pruebas no es una variable instrumentada en este estudio (ver alcance en `TESTS_ARQUITECTONICOS.md` / §7 del anteproyecto); no se interpreta como degradación arquitectónica.
## Notas del investigador
## Notas del investigador
- La regla S108 y el resto del Quality Profile de Kotlin no reportaron code smells nuevos en el adaptador en memoria ni en la clase `@Configuration`: los 2 issues abiertos del proyecto están ambos en `build.gradle.kts` y ya existían en P-001, sin relación con `Collections.synchronizedMap` ni con la clase de configuración. Cierre pendiente aún referido a si S108 cubre formalmente el caso de bloque `catch` vacío (ver ítem abierto general del proyecto), pero no aplica a los archivos de esta sesión.
- Se consolida el uso de un puerto único `AcademicDataRepository` (fiel a `DESIGN_SGVA.md` §3) como decisión de diseño válida para esta versión; no se identifica necesidad de segregarlo en esta etapa. Se revisará si el crecimiento de casos de uso en V2 justifica separar interfaces por entidad.
- Segunda instancia (tras P-001) de decisión arquitectónica autónoma consistente con el contexto documentado en lugar de la alternativa más obvia — dato relevante para la taxonomía de comportamiento de la IA bajo restricciones arquitectónicas formales (Resultados Esperados, punto 2).
