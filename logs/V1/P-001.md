# P-001

**Iteración:** V1
**Herramienta:** Claude Code CLI
**Tipo:** Generación inicial
**Fecha:** 2026-08-27 04:15
**Session ID:** e5287f2b-3d94-4765-b4ba-366009c3c6b2
**Transcript:** /home/stevkazt/.claude/projects/-home-stevkazt-SGVA/e5287f2b-3d94-4765-b4ba-366009c3c6b2.jsonl

## Contexto previo
Repositorio con el andamiaje base de P-000 (`build.gradle.kts`, `ArchitectureTest.kt`, `DomainMarker.kt`). Dominio vacío salvo el marcador. Primera sesión de generación asistida por IA de la investigación (V1).

## Secuencia de interacción
### Prompt 1
"Necesito modelar las entidades de negocio para gestionar estudiantes y docentes en el sistema de autoevaluación académica. Un estudiante tiene un identificador, programa, facultad, cohorte (formato AAAAS), un estado (matriculado, graduado o desertor) y opcionalmente un puntaje de Saber Pro entre 0 y 300. Un docente tiene identificador, facultad, nivel de formación (especialización, maestría o doctorado), tipo de dedicación (tiempo completo, medio tiempo o cátedra) y el periodo académico (formato AAAAS). Impleméntalo de forma que las reglas de validación de formato y rango se apliquen correctamente."

**Resumen de la respuesta de Claude Code CLI:**

Modeló ambas entidades en `com.example.sgva.domain`, respetando el aislamiento del dominio (Kotlin puro, sin Spring/Jakarta/Jackson) y las convenciones de `CLAUDE.md`.

- **Entidades y enums:** `Estudiante.kt` (data class: id, programa, facultad, cohorte, estado, `puntajeSaberPro: Int?`), `Docente.kt` (data class: id, facultad, nivelFormacion, dedicacion, periodo), `EstadoEstudiante.kt`, `NivelFormacion.kt`, `TipoDedicacion.kt`.
- **Validación:** `ReglasDeFormato.kt` centraliza el chequeo del formato `AAAAS` (regex `^\d{4}[12]$`, tomada de `DESIGN_SGVA.md` §0.1) y de campos obligatorios, compartido entre `cohorte` y `periodo`. `ExcepcionesDominio.kt` define `DominioException` (base, extiende `IllegalArgumentException`) y las específicas `FormatoPeriodoInvalidoException`, `PuntajeSaberProFueraDeRangoException`, `CampoRequeridoVacioException`.
- **Mecanismo de validación:** las invariantes se verifican en el bloque `init` de cada data class, de forma que una instancia corrupta no puede llegar a existir; la validación vive en el dominio, no delegada a infraestructura, conforme a §0.1.
- **Test:** `EntidadesDominioTest.kt` con 12 casos cubriendo límites y rechazos.
- **Verificación reportada por la IA:** `EntidadesDominioTest` 12 tests / 0 failures; `ArchitectureTest` 8 tests / 0 failures.
- **Decisión autónoma relevante:** la IA notó por su cuenta que los fixtures CSV usan formato `2021-1`/`2026-1` (con guion) en vez de `AAAAS`, y explícitamente dejó la normalización como responsabilidad del futuro adaptador de `DocumentParserPort`, en lugar de relajar la validación del dominio o modificar el fixture. Esta observación coincide con el hallazgo identificado independientemente por el investigador (ver sección de archivos modificados).
- No realizó commit — quedó a la espera de confirmación del investigador, consistente con el protocolo de commits manuales establecido para este proyecto.

## Archivos generados o modificados
Confirmado mediante `git status` tras la sesión:

**Generados por Claude Code CLI en esta sesión (archivos nuevos, sin tracking previo):**
- `src/main/kotlin/com/example/sgva/domain/Estudiante.kt`
- `src/main/kotlin/com/example/sgva/domain/Docente.kt`
- `src/main/kotlin/com/example/sgva/domain/EstadoEstudiante.kt`
- `src/main/kotlin/com/example/sgva/domain/NivelFormacion.kt`
- `src/main/kotlin/com/example/sgva/domain/TipoDedicacion.kt`
- `src/main/kotlin/com/example/sgva/domain/ReglasDeFormato.kt`
- `src/main/kotlin/com/example/sgva/domain/ExcepcionesDominio.kt`
- `src/test/kotlin/com/example/sgva/domain/` (carpeta nueva, contiene las pruebas unitarias del dominio generadas por la IA)

**Modificado — diff confirmado como trivial, no atribuible a Claude Code CLI:**
- `src/test/resources/fixtures/datos_estudiantes_test.csv` — el `git diff` mostró únicamente la normalización del salto de línea final del archivo (`\ No newline at end of file` → con salto), sin cambios de contenido. No hay evidencia de que Claude Code CLI haya modificado los datos de este fixture en la sesión de P-001; se descarta la hipótesis de desbordamiento de alcance sobre este archivo.
  - ⚠️ **Hallazgo independiente (no atribuible a esta sesión), detectado de forma convergente por el investigador y por la propia IA:** al revisar el contenido del fixture se detectó que usa el formato `AAAA-S` con guion (ej. `2023-1`, `2020-2`) para el campo `cohorte`, mientras que `DESIGN_SGVA.md` Sección 0.1 exige el formato `AAAAS` sin guion (regex `^\d{4}[12]$`). Este fixture preexistía a P-001. Notablemente, Claude Code CLI identificó esta misma inconsistencia por su cuenta durante la sesión (ver Secuencia de interacción) y tomó la decisión autónoma de no modificar el dominio ni el fixture, delegando la normalización al futuro adaptador de `DocumentParserPort` — una decisión consistente con el aislamiento del dominio exigido en `DESIGN_SGVA.md`. Aun así, el fixture debe corregirse antes de usarse en pruebas de integración de parsing de CSV en futuras iteraciones, para evitar fallos de validación mal atribuidos.

**Modificados manualmente por el investigador (fuera de esta sesión de IA, confirmado que Claude Code CLI no los tocó):**
- `src/test/kotlin/com/example/sgva/ArchitectureTest.kt` — corrección de la regla de nomenclatura de puertos (aceptar `Repository` o `Port`), aplicada antes de esta sesión.
- `TESTS_ARQUITECTONICOS.md` — actualización de la justificación metodológica (Jakarta/javax, nomenclatura de puertos, nota sobre S108).

**Modificado automáticamente por el hook `SessionEnd`:**
- `PROMPT_LOG.md` — inserción del borrador de fila para P-001 en la tabla índice.
- `logs/V1/` — carpeta nueva con `P-001.md`.

## Resultado de medición
- **ArchUnit:** PASS (`./gradlew test` → `BUILD SUCCESSFUL`, incluye las 8 pruebas de `ArchitectureTest.kt` sin fallos reportados).
- **SonarQube (Overall Code):**
  - **Mantenibilidad (Deuda Técnica):** 2 code smells abiertos — Calificación **A** (Ratio de Deuda Técnica ≤ 5%, según umbral SQALE definido en el anteproyecto). Baseline saludable para el inicio de V1.
  - Seguridad: 1 issue abierto, calificación B (fuera del alcance medido por esta investigación; se registra solo como contexto).
  - Confiabilidad: 0 issues, calificación A (fuera del alcance medido).
  - Duplicación: 0.0%. Cobertura de pruebas: 0.0% (no se mide en este estudio; no hay herramienta de cobertura tipo JaCoCo integrada, y no es un atributo dentro del alcance declarado en el anteproyecto).
  - TODO — opcionalmente, registrar el porcentaje exacto del Technical Debt Ratio (no solo la calificación A) desde `Measures → Maintainability`, para mayor granularidad al comparar contra V2/V3/V4.

## Notas del investigador
- El análisis de SonarQube reportó "missing blame information" para los archivos del dominio y de pruebas. Se confirmó que esto se debe a que los archivos generados en esta sesión (y algunos ajustes previos a `ArchitectureTest.kt` realizados manualmente) aún no habían sido comprometidos a Git — no indica ninguna modificación de `ArchitectureTest.kt` por parte de Claude Code CLI durante esta sesión.
- **Acción tomada:** se realizó el primer commit del proyecto siguiendo el formato de Conventional Commits de `CLAUDE.md`, estableciendo así un punto de referencia limpio en Git para que el cálculo de "New Code" en SonarQube distinga correctamente el código de cada iteración futura (V2, V3, V4).
- La pestaña "New Code" del dashboard mostró inicialmente 0 issues nuevos pese a que la mayoría del código era nuevo de esta sesión — consistente con la falta de historial de Git al momento del primer análisis. Se recomienda volver a correr `./gradlew sonar` después de cada commit para verificar que "New Code" quede correctamente delimitado en adelante.
